query blogOverviewItem($slug: ID!) {
  BlogoverviewItem(id: $slug,  resolve_links: "url") {
    content {
      _editable
      _uid
      featured_post {
        content
        firstPublishedAt
        slug
        id
      }
      newsletter_subtitle
      newsletter_title
      subtitle
      title
    }
    uuid
  }
}
