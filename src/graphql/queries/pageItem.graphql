query pageItem($slug: ID!) {
  PageItem(id: $slug, resolve_relations: "price_selector.price", resolve_links: "url") {
    content {
      seo
      body
      component
      _editable
    }
    uuid
  }
}